#pragma once
#include "Matrix.h"
#include <iostream> 
#include <stdlib.h> 
#include <math.h> 

/*
class layer
{
public:
	layer(string filter, string activa, float size[]);
	void flatten();
	void Dense();
	Matrix Conv2D(Matrix input, string filter, string activa);

private:
	Matrix weight;
};
*/
